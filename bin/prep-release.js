#!/usr/bin/env node
"use strict";

console.error(
  "Wcash Wallet public release preparation is disabled while the desktop packages are Local Regtest QA candidates. " +
    "Versioning, signing identities, and publishing must be approved in a separate release change.",
);
process.exit(1);
